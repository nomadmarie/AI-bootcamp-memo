# 통계값
 # info 
 # describe

# 데이터 타입 및 종류(유형)
 # 평균
 # 합계
 # 통계량

# groupby
# query
# filtering

# 개념 정리

# EDA
 # 데이터의 대푯값을 구하는 통계치는 평균(mean), 중앙값(median), 최빈값(mode) 등입니다.
 # 데이터가 흩어진 정도를 파악할 수 있는 통계치는 표준편차(standard deviation), range, IQR 등입니다.
 # Histogram으로 데이터의 분포를 살펴봅니다. 

# 데이터 타입
 # 수치형 데이터는 평균, 표준편차, 최댓값, 최솟값 등의 통계치를 통해 분석할 수 있습니다.
 # 데이터의 빈도, 비율 등의 통계치는 범주형 데이터에 적합합니다.

